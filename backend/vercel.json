{
  "version": 2,
  "builds": [
    { "src": "src/api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    {
      "src": "^/api/(.*)",
      "dest": "/src/api/index.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"]
    }
  ]
}